<div class=show.box>
  <% if flash[:notice] %>
    <p id="notice">
      <%= flash[:notice] %>
    </p>
    <% end %>
  <h3>Title</h3>
  <p><%= @book.title %></p>
  <h3>Body</h3>
  <p><%= @book.body %></p>
</div>
<%= link_to "Edit", edit_book_path(@book.id) %>
<%= link_to "Back", books_path %>

